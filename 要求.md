音频转 MIDI 桌面应用开发任务计划
目标：开发一款独立桌面软件，支持用户拖拽/选择音频文件，经自动转录后生成并导出 MIDI 文件，并可预览播放。

选用技术栈：

语言：Python 3.10+

GUI：PySide6 (Qt for Python)

音频处理：librosa, soundfile

转录引擎：Spotify basic-pitch (后续转为 ONNX 模型 + onnxruntime 推理，减少体积)

可选音源分离：demucs

MIDI 生成：pretty_midi

播放预览：pygame (+ FluidSynth 进行 MIDI 合成)

打包：PyInstaller

1. 项目初始化
1.1 创建项目目录结构

text
audio2midi/
├── main.py                 # 应用入口
├── ui/
│   ├── main_window.py      # 主窗口逻辑
│   ├── main_window.ui      # Qt Designer 界面文件(可选)
│   └── resources.qrc       # 图标等资源
├── core/
│   ├── audio_loader.py     # 音频加载与预处理
│   ├── transcriber.py      # 转录引擎封装
│   ├── postprocess.py      # 音符后处理
│   ├── midi_generator.py   # MIDI 生成与导出
│   └── player.py           # 音频/MIDI 播放
├── models/                 # 存放 ONNX 模型文件(运行时打包)
├── requirements.txt
└── README.md
1.2 依赖清单 (requirements.txt)

text
pyside6>=6.5
librosa
soundfile
resampy
pretty_midi
pygame
numpy
onnxruntime
# 可选：basic-pitch 的原始包仅用于导出 ONNX，日常运行不依赖 tensorflow
# tensorflow (仅用于模型转换阶段)
1.3 环境初始化命令

bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
2. 用户界面构建 (PySide6)
2.1 主窗口布局

顶部：菜单栏 (文件、设置、帮助)

中央区域：拖拽文件区域，显示当前文件名、时长、采样率

选项面板：转录阈值、最小音符时长、是否启用音源分离 (复选框)

底部：进度条 + 转录按钮 + 播放原始音频/合成 MIDI 按钮 + 导出 MIDI 按钮

状态栏：显示当前操作提示

2.2 拖拽功能
利用 QWidget.dragEnterEvent 和 dropEvent 支持文件拖入，过滤音频后缀 (.wav, .mp3, .flac, .ogg)。

2.3 多线程处理
所有耗时操作（加载、转录、音源分离）必须在 QThread 中执行，通过信号更新进度和结果，防止界面冻结。

2.4 设置对话框
提供阈值滑动条、音源分离模型选择 (默认 4 音轨 demucs) 等，保存到 QSettings。

3. 音频加载与预处理模块 (core/audio_loader.py)
功能：

接收文件路径，使用 librosa.load() 加载音频，统一重采样为 22050 Hz 单声道 (basic-pitch 要求)。

返回音频数据数组 (float32) 与原始采样率。

支持大文件分块加载？(初期可全量加载，若内存不足再优化)

封装进度回调信号，即可报告加载进度。

4. 转录引擎集成 (core/transcriber.py)
阶段 4.A：直接使用 basic-pitch Python 包 (快速原型)

安装 basic-pitch (会同时安装 TensorFlow)，调用其 API：

python
from basic_pitch.inference import predict
from basic_pitch import ICASSP_2022_MODEL_PATH
model_output, midi_data, note_events = predict(audio, sr=22050)
获取 model_output (音高、起止、激活度) 或直接使用 note_events 列表。

注意：界面线程需启动子进程或 QThread 调用此函数，并捕获进度打印 (如 tqdm) 转为信号。

阶段 4.B：转换为 ONNX 推理（最终发布版）

使用 basic-pitch 的预训练 TensorFlow SavedModel，通过 tf2onnx 转换为 ONNX。

保存 ONNX 模型到 models/basic_pitch.onnx。

编写 ONNX 推理代码：

输入：音频 Mel 频谱 (需自己用 librosa 生成)

输出：三个头的特征图 (轮廓、音符 onset/offset 等)

后处理解码（需实现或复用 basic-pitch 的 PyTorch/TF 解码逻辑的纯 numpy 版本），从特征图提取音高、起止时间。此步骤可以抄 basic-pitch 开源代码中的后处理部分 (inference.py)，去掉 TF 依赖，用 numpy 实现。

接口建议：无论底层用 TF 还是 ONNX，对外暴露方法 transcribe(audio: np.ndarray, sr: int) -> list[Note]，其中 Note 包含 pitch, start_time, end_time, velocity。

5. 音源分离预处理 (可选功能)
集成 demucs 库 (PyTorch 版本)。

当用户在界面勾选“启用音源分离”，先用 demucs 将音频分离为：vocals, bass, drums, other。

按配置选择要转录的轨道（例如仅 other 含伴奏，或全部）。

对每个选定轨道调用转录引擎，最后合并结果（需处理时间偏移，各轨时间线一致）。

此功能耗时较长，务必在 QThread 中运行，并传递进度 (demucs 自带进度回调)。

6. 后处理与 MIDI 生成 (core/postprocess.py & core/midi_generator.py)
6.1 音符后处理

过滤时长极短的音符 (默认 < 50ms)。

合并相邻同音高且间隔极短的音符。

限制最小力度。

可调参数从 UI 传入。

6.2 MIDI 生成

将处理后的 Note 列表传入 pretty_midi.PrettyMIDI。

创建一个 Instrument 轨道 (默认钢琴，program=0)。

逐音符添加 note_on/note_off 事件，设置 velocity。

设置全局速度 (默认 120 BPM 或由用户指定)。

导出文件使用 pretty_midi.write('output.mid')。

提供保存对话框 (QFileDialog) 获取保存路径。

7. 音频/MIDI 预览播放 (core/player.py)
原始音频播放：使用 pygame.mixer.music，可快速播放 wav/mp3。

MIDI 合成播放：使用 pygame.mixer.music 无法直接播放 MIDI，可采用：

方案 A：使用 fluidsynth 库 (pyFluidSynth) 配合 SoundFont 文件 (.sf2) 实时合成音频再送入 pygame 或直接输出。

方案 B：将生成的 MIDI 用 pretty_midi.synthesize() 或者 pydub + timidity 合成到 wav 临时文件，再用 pygame 播放。

建议采用方案 A，并将 SoundFont 文件（例如 GeneralUser GS v1.471.sf2）作为资源打包。

播放控制：开始/暂停/停止，进度条显示。

8. 主流程串联 (main.py 与 main_window.py)
8.1 主要操作流程

用户拖入音频 → 显示音频信息 (时长、波形缩略图可选)。

点击“转录” → 检查音频加载完成 → 禁用按钮，启动转录线程。

线程执行：
a. (可选) 进行音源分离 → 更新进度。
b. 对每个目标音频调用转录引擎 → 更新进度。
c. 合并/后处理音符 → 更新进度。

线程完成 → 将 MIDI 数据实例与 Note 列表存入主窗口变量 → 恢复按钮。

用户可点击“播放 MIDI”或“导出 MIDI”。

8.2 异常处理

捕获加载、转录、分离等过程异常，通过信号传递错误信息，在界面上弹出 QMessageBox 提示。

9. 打包与发布
9.1 PyInstaller 配置

制作 .spec 文件，显式包含：

隐藏导入：soundfile, resampy, pretty_midi, onnxruntime 等。

数据文件：models/basic_pitch.onnx、SoundFont 文件、UI 资源。

添加图标 (Windows .ico, macOS .icns)。

测试打包后的 exe 是否缺少动态库 (如 libsndfile)。

9.2 体积优化

不用 TensorFlow，仅保留 ONNX Runtime (~30MB)。

若暂时保留 demucs (Pytorch)，则体积会较大 (~200MB+)，可考虑提供分离的安装版或不包含 demucs 的轻量版。

最终目标：基础版安装包控制在 150 - 200 MB 之间。

10. 开发阶段建议
阶段	主要任务	产出
第 1 周	环境搭建、UI 框架、文件拖放、简单的音频加载与播放	可运行的界面壳子
第 2 周	集成 basic-pitch (非 ONNX)，实现转录+MIDI生成导出	核心功能可用，体积大
第 3 周	添加多线程进度、MIDI 合成预览、参数调节面板	完整体验的应用
第 4 周	模型转 ONNX，优化推理与打包，添加异常处理	可发布版本
后续迭代	音源分离集成、批处理、实时转录等高级功能	增强版
注意事项给 TraeCN/GLM5：

请使用面向对象编程，每个模块独立成类。

所有文件读写、长时间计算务必放在 QThread，通过信号更新界面。

转录引擎的模型推理结果解码需要仔细对齐采样点时间，确保 MIDI 音符时间准确。

代码注释清晰，方便后续优化。